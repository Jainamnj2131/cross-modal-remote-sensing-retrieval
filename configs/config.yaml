# BigEarthNet Data Pipeline Configuration
data:
  dataset_dir: "D:/Dataset"
  metadata_path: "D:/Dataset/metadata.parquet"
  batch_size: 16
  num_workers: 2
  # Sentinel-2 band selection (RGB for Week 2 debugging; can easily be changed later)
  bands:
    - "B04"
    - "B03"
    - "B02"
